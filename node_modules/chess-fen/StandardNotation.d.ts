import { Color, MoveArgs, Piece, PieceData } from "./types";
export declare type FindPiecesMovableTo = (to: string, piece: Piece) => PieceData[];
export declare class StandardNotation {
    private readonly move;
    private readonly piece;
    private readonly from;
    private readonly to;
    private readonly promotion?;
    private static readonly specialMoves;
    private static readonly pieces;
    constructor(move: string, findPiecePieceMovableTo?: FindPiecesMovableTo);
    toMoveArgs(toMove?: Color): MoveArgs;
    private findFrom;
    private static toPiece;
    private static removeSpecialCharacters;
}
export default StandardNotation;
