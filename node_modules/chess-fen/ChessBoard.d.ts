import { Color, ControlledSquares, PieceData, BoardContent, SquareData } from "./types";
export declare class ChessBoard {
    readonly controlledSquares: ControlledSquares;
    readonly map: Map<string, SquareData>;
    private currentPosition;
    private currentColor;
    private opponentColor;
    private readonly board;
    constructor(board: BoardContent[][]);
    getPiecesMovableTo(coordinate: string): PieceData[];
    isControlledSquare(coordinate: string, color?: Color): boolean;
    get(coordinate: string): SquareData;
    getPiece(coordinate: string): PieceData;
    private traversePositions;
    private mapChessBoard;
    private getOpponentControlledSquares;
    private getRowControlledPositions;
    private getColumnControlledPositions;
    private getDiagonalControlledPositions;
    private getPawnControlledPositions;
    private getKnightControlledPositions;
    private getKingControlledPositions;
    private getPawnMoves;
    private isEmptyOrOccupiedByOpponentPiece;
    private hasOpponentPiece;
    private isOccupiedPosition;
    private isInsideBoard;
    private getControlledPositionsUntilObstructed;
    private validPositions;
    private static unique;
}
export default ChessBoard;
